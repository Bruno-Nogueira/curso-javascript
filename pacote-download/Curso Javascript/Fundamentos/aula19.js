const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) // raio elevado ao quadrado
console.log(area.toFixed(2))

console.log(typeof Math)
// Math é um objeto, e ele tem uma constante chamada PI e tem uma função que calcula a potência (pow)
